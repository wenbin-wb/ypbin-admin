server:
  port: 18084

spring:
  application:
    name: ypbin-job
  cloud:
    nacos:
      server-addr: ${NACOS_ADDR:localhost:8848}
  config:
    # 公共配置（Nacos 共享：限流等），本地无 Nacos 配置中心时回退默认
    import: optional:nacos:ypbin-common.yaml

ypbin:
  tenant:
    enabled: true
  data-permission:
    enabled: true
