server:
  port: 18084

spring:
  application:
    name: ypbin-miniapp
  cloud:
    nacos:
      server-addr: ${NACOS_ADDR:localhost:8848}
  config:
    import:
      - optional:nacos:ypbin-common.yaml
      - optional:nacos:ypbin-miniapp.yaml
