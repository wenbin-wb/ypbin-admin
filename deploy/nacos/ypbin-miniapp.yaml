# ============================================================
-- ypbin-miniapp 服务配置（Nacos 配置中心）
-- Data ID: ypbin-miniapp.yaml | Group: DEFAULT_GROUP | 格式: YAML
# ============================================================

ypbin:
  security:
    interceptor: false
    excludes:
      - /actuator/**
