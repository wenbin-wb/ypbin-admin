<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>cn.ypbin.admin</groupId>
        <artifactId>ypbin-service-api</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>ypbin-system-api</artifactId>
    <name>${project.artifactId}</name>
    <description>微服务版 ypbin-system-api：Feign 接口与共享 DTO</description>

    <dependencies>
        <!-- 统一响应体 R / 错误码（Feign 返回结构） -->
        <dependency>
            <groupId>cn.ypbin</groupId>
            <artifactId>ypbin-starter-cloud-core</artifactId>
        </dependency>
        <!-- 序列化注解（@RefText/@JsonSerialize 等） -->
        <dependency>
            <groupId>cn.ypbin</groupId>
            <artifactId>ypbin-starter-json</artifactId>
        </dependency>
        <!-- 缓存（SysCache 跨服务查询缓存） -->
        <dependency>
            <groupId>cn.ypbin</groupId>
            <artifactId>ypbin-starter-cache</artifactId>
        </dependency>
        <!-- 敏感词注解（DTO 字段） -->
        <dependency>
            <groupId>cn.ypbin</groupId>
            <artifactId>ypbin-starter-sensitive-words</artifactId>
        </dependency>
        <!-- Excel 注解（VO 导入导出字段） -->
        <dependency>
            <groupId>cn.ypbin</groupId>
            <artifactId>ypbin-starter-excel</artifactId>
        </dependency>
        <!-- 分页基类（Query 继承 PageQuery） -->
        <dependency>
            <groupId>cn.ypbin</groupId>
            <artifactId>ypbin-starter-extension-crud</artifactId>
        </dependency>
        <!-- 实体基类/MyBatis 注解（共享实体依赖） -->
        <dependency>
            <groupId>cn.ypbin</groupId>
            <artifactId>ypbin-starter-data</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.ypbin</groupId>
            <artifactId>ypbin-starter-extension-tenant</artifactId>
        </dependency>
        <!-- Feign 接口定义 -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-openfeign</artifactId>
            <version>4.2.0</version>
        </dependency>
    </dependencies>
</project>
