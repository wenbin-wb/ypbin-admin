# ypbin License 商业授权使用说明书

> 面向「按授权交付」的商业软件，一套完整的授权签发与校验方案。本文档写给两类人：
> - **签发端运营者**：用管理台签发、管理授权（第 2–4 章）；
> - **消费端开发者**：把自己的软件接入授权校验，保护功能（第 5–7 章）。
>
> 配套示例：`ypbin-license-demo`（最小可运行的消费端参考应用）。端到端测试步骤见 `LICENSE-TEST.md`。

## 目录

- [1. 概览](#1-概览)
- [2. 核心概念](#2-核心概念)
- [3. 签发端：部署与准备](#3-签发端部署与准备)
- [4. 签发端：签发与管理授权](#4-签发端签发与管理授权)
- [5. 消费端：接入你的软件](#5-消费端接入你的软件)
- [6. 联机校验：吊销实时闭环](#6-联机校验吊销实时闭环)
- [7. 错误码对照](#7-错误码对照)
- [8. 安全须知](#8-安全须知)
- [9. 常见问题](#9-常见问题)
- [10. 术语表](#10-术语表)

---

## 1. 概览

### 1.1 双端模型

License 体系分两端，职责严格分离：

```
┌─────────────────────────────┐        ┌──────────────────────────────┐
│  签发端（供应方 = admin）      │        │  消费端（被授权方 = 你的软件）    │
│                             │  交付   │                              │
│  管理台：签发 / 审批 / 吊销    │────────▶  引入 ypbin-starter-license，  │
│  持有 SM2 私钥 + SM4 密钥     │ 授权串  │  启动离线验签、@LicenseCheck 拦截 │
│                             │        │  只配 SM2 公钥 + SM4 密钥       │
│  ── 可选 ──                  │        │  ── 可选 ──                    │
│  联机校验接口（回验吊销）       │◀───────│  定期 / 登录 / 调用时联机回验     │
└─────────────────────────────┘        └──────────────────────────────┘
```

| | 签发端 admin | 消费端（你的应用） |
|---|---|---|
| 角色 | 供应方，管理授权生命周期 | 被授权方，运行授权校验 |
| 引入模块 | 整个 `ypbin-admin` | `ypbin-starter-license`（+ 可选 `-job` `-security`） |
| 密钥 | SM2 私钥 + 公钥 + SM4 密钥 | 只配 SM2 **公钥** + SM4 密钥 |
| 是否需联网 | 否（联机接口可选） | 离线可校验，联机回验可选 |

### 1.2 一次完整的授权生命周期

```
① 签发端创建草稿 DRAFT → ② 提交 PENDING → ③ 他人审批 ISSUED（产出授权串）
      → ④ 交付（授权码 / 授权文件） → ⑤ 消费端放入授权串、启动即验签
      → ⑥ 运行期：过期/宽限/非法被拦截 → ⑦（可选）吊销，消费端联机感知
```

### 1.3 技术要点

- **国密体系**：SM2 签名验签（防篡改）、SM4-GCM 对称加密（授权串加密传输）、SM3 机器指纹摘要（绑定机器）。
- **离线可用**：验签全在消费端本地完成，不依赖签发端在线；只有吊销需要联机（可选）。
- **授权串格式**：压缩 + base64url + 版本号，内联授权码上限 768 字符。

---

## 2. 核心概念

### 2.1 授权内容（签发时填写）

| 字段 | 含义 | 留空时 |
|---|---|---|
| 被授权方 `subject` | 授权给谁（公司/产品名），必填 | — |
| 交付模式 `deliveryMode` | `CODE` 内联授权码 / `FILE` 授权文件 | — |
| 授权租户 `tenantId` | 绑定哪个租户 | 不限租户 |
| 生效时间 `effectiveAt` | 早于此时间视为未生效 | 立即生效 |
| 到期时间 `expireAt` | 到期后进入宽限期 | 永久授权 |
| 宽限天数 `graceDays` | 到期后仍可运行的宽限天数 | 0（到期即停） |
| 机器指纹 `fingerprints` | 允许运行的机器指纹列表，可多条 | 不限机器 |
| 功能模块 `modules` | 授权的功能模块标识集合 | 不限模块 |
| 额度限制 `quotas` | 业务额度上限（键值对，如 `device=100`） | 不限制 |
| 扩展参数 `attributes` | 自定义键值对，消费端可读取 | 无 |
| 备注 `remark` | 供应方备注 | 无 |

> 机器指纹从消费端 `/fingerprint`（或代码 `MachineFingerprint.current()`）取得，签发时填进去即完成机器绑定。

### 2.2 额度限制与扩展参数

`quotas` 与 `attributes` 是授权内容里两个「随授权打包」的灵活载体，都在签发时填入、验签后由消费端读取，签名保证不可篡改。

**额度限制 `quotas`（业务计数上限）**：键为额度名、值为上限，例如：

```json
{ "device": 100, "user": 500, "api-request": 100000 }
```

- 定义的是**契约上限**：签进授权串、随授权防篡改，消费端读到的值一定是签发时的原样；
- **实际使用计数由消费端自己维护并强制**：`quotas` 只回答「上限是多少」，不负责计数。业务侧在新增设备/用户时自增计数，超过上限即拦截（`quota("device")` 用法见 5.5）；
- 常见场景：按设备数/用户数授权（`device` / `user`）、按调用量计费（`api-request`）、按并发实例数限流。

**扩展参数 `attributes`（能力开关/版本标识）**：业务侧自解释的键值对，例如：

```json
{ "edition": "enterprise", "support-level": "gold" }
```

- 常见的「免费版/专业版/企业版」差异即由此驱动：`edition=enterprise` 开放高级功能、`support-level=gold` 决定工单响应等级；
- 与额度不同，它不强制任何计数，只是把「买的是哪个版本/服务」固化到授权里，消费端按值分支（`attribute("edition")` 用法见 5.5）。

### 2.3 状态机

**审批状态**（签发端管理台的 `approve_status`）：

```
DRAFT(草稿) → PENDING(待审批) → ISSUED(已签发)
                    │            └→ REVOKED(已吊销)
                    └→ REJECTED(已驳回) → 重新编辑 → DRAFT
```

**运行态**（消费端按当前时间实时判定，`LicenseStatus`）：

| 状态 | 是否可用 | 含义 |
|---|---|---|
| `LEGAL` | ✅ | 合法可用：验签通过且在生效期内（或永久授权） |
| `GRACE` | ⚠️ | 非法可用：已过期但在宽限期内 |
| `ILLEGAL` | ❌ | 非法不可用：无授权/验签失败/指纹不符/未生效/过期宽限耗尽/时钟回拨 |

### 2.4 交付模式

- **授权码 `CODE`**：签发后复制一段 Base64 授权串，直接发给对方（适合精简授权，上限 768 字符）。
- **授权文件 `FILE`**：下载 `.lic` 文件，内容即授权串（内容复杂时建议用这个）。

> 内容较多（大量模块/额度/属性）时授权码可能超长，签发时后端会报错并引导改用授权文件。

---

## 3. 签发端：部署与准备

### 3.1 环境要求

| 依赖 | 说明 |
|---|---|
| MySQL | admin 数据源，Flyway 启动时自动建表迁移 |
| Redis | admin 依赖 |
| JDK 17+ | 构建运行 |

### 3.2 启动与初始账号

```bash
mvn -f ypbin-admin-server/pom.xml spring-boot:run   # 默认 8080
```

内置两个超管（互为审批复核人，密码均 `admin123`）：

| 账号 | 角色 | 用途 |
|---|---|---|
| `admin` | 超级管理员 | 创建、提交、吊销授权 |
| `approver` | 超级管理员（V2 种子） | **审批签发**（审批人必须 ≠ 创建人，实现双人复核） |

> 生产环境请立即修改默认密码。

### 3.3 配置签发密钥（必做，首次）

授权签发需要 SM2 私钥/公钥 + SM4 密钥，**经环境变量注入、不落库**：

```bash
# .env（参考 .env.example），IntelliJ EnvFile 插件或 shell export 注入
LICENSE_ISSUER_PUBLIC_KEY=<SM2 公钥 Base64>
LICENSE_ISSUER_PRIVATE_KEY=<SM2 私钥 Base64>
LICENSE_ISSUER_SM4_KEY=<SM4 密钥 Base64>
```

获取密钥对的方式：

1. 启动 admin，登录后进入 **授权管理 → 授权列表**，点右上角 **生成密钥**；
2. 弹窗一次性展示 `publicKey / privateKey / sm4Key`（**不落库，仅此一次可见，务必离线保管**）；
3. 把三项填入 `.env` 对应变量，重启 admin 生效。

> **私钥只属于签发端**。消费端配置的是 SM2 公钥 + SM4 密钥（同一套密钥对，见第 5 章），私钥绝不外发。

### 3.4 注册联机校验应用（可选）

若要让消费端联机回验吊销，先为每个消费端应用准备好开放应用 AK/SK。**多数情况无需手工建**：审批通过签发授权时，系统会按被授权方 `subject` 自动创建/复用开放应用，AK/SK 随交付信息展示（见 4.4）；如需独立应用（与授权解耦、多产品共用一个客户端身份），再手工建：

1. 登录 admin → **授权管理 → 开放应用** → **新建**；
2. 填应用名称（如 `客户A-生产`），保存后自动生成 AK/SK——Access Key 是公开的应用标识、Secret Key 是参与签名的私有密钥，**仅展示一次**；
3. 把 AK/SK 交付给该消费端，配到其 `ypbin.license.online.access-key / secret-key`（见 6.1），联机请求即以该应用的密钥做接口签名鉴权。

> 内置演示应用「ypbin-license-demo 联机校验」已由 V2 种子创建，AK/SK 与 demo 默认配置一致，仅开发/演示用。生产请用签发自动创建或手工新建的应用配独立 AK/SK：某应用密钥泄露只影响该应用，可在应用管理**重置密钥**或**禁用**，不影响其他消费端。

---

## 4. 签发端：签发与管理授权

> 操作入口：菜单 **授权管理 → 授权列表**。全程走双人复核：一个人创建提交，另一个人审批签发。

### 4.1 新建草稿

1. 点右上角 **新建授权**；
2. 按 [2.1 授权内容](#21-授权内容签发时填写) 填表（参考建议：到期时间填未来日期、宽限 3 天、功能模块填消费端要保护的模块标识）；
3. 保存 → 列表出现 **草稿 DRAFT** 行。

> DRAFT 行可用：编辑 / 提交 / 删除；不可用：审批 / 吊销 / 查看授权码 / 下载。

### 4.2 提交审批

DRAFT 行点 **提交** → 状态变 **待审批 PENDING**。

> PENDING 行只剩「审批」；编辑/提交/删除按钮消失。

### 4.3 审批签发（双人复核）

1. **退出 admin，用 `approver` 登录**（审批人不能是创建人）；
2. 找到该行 → 点 **审批** → 选 **通过**；
3. 状态变 **已签发 ISSUED**，行内「当前状态」列实时显示运行态（合法可用/未生效等）。

> 用 `admin` 自己审批会报「审批人不能是授权创建人，须由他人复核签发」。

### 4.4 交付给消费端

按交付模式二选一（严格对应，反用报错）：

- **授权码**：ISSUED 行 → 更多 → **查看授权码** → 复制弹窗里的 Base64 授权串，发给对方。
- **授权文件**：ISSUED 行 → 更多 → **下载** → 得到 `<licenseId>.lic` 文件（内容即授权串）。

> 交付弹窗同时展示**签发时按被授权方自动创建的联机应用 AK/SK**（同主体复用、续期不重建，AK/SK 对客户保持稳定），连同授权串一起发给消费端，无需再去应用管理手工建。FILE 交付下也可用「更多 → **联机应用信息**」单独查看这份 AK/SK。

### 4.5 其他管理操作

| 操作 | 状态前提 | 说明 |
|---|---|---|
| 编辑 | DRAFT / REJECTED | 改字段后需重新提交审批 |
| 驳回 | PENDING | 审批时选驳回，**必须填驳回原因** |
| 吊销 | ISSUED | 停止已下发授权的有效性（消费端需联机才感知，见第 6 章） |
| 删除 | DRAFT / REJECTED | 已流转的授权不可删 |

### 4.6 权限控制

授权模块的菜单/按钮权限码：菜单 `system:license:list`，按钮 `system:license:add/edit/submit/approve/revoke/genkey`。给角色分配后即可按权限操作（超管默认全有）。

---

## 5. 消费端：接入你的软件

> 目标：把你的软件加上「没有有效授权就拦截受保护能力」。参考实现看 `ypbin-license-demo`。

### 5.1 引入依赖

```xml
<dependency>
    <groupId>cn.ypbin</groupId>
    <artifactId>ypbin-starter-license</artifactId>
</dependency>
```

> 通过 `ypbin-starter-bom` 引入（见 starter 文档）。联机回验可追加 `ypbin-starter-job`（定期任务）与 `ypbin-starter-security`（登录回验），见第 6 章。

### 5.2 配置密钥与授权文件

消费端**只需公钥 + SM4 密钥**（同一套密钥对，来自签发端），授权串放本地文件：

```yaml
ypbin:
  license:
    enabled: true
    public-key: ${LICENSE_PUBLIC_KEY:MFkwEwYHKoZIzj0CAQYIKoEcz1UBgi0DQgAE...}   # SM2 公钥，与签发端同一套
    secret-key: ${LICENSE_SM4_KEY:SMMTzfogKfcZFxZtUGmZVg==}                    # SM4 密钥
    location: ./license.dat            # 授权串文件路径
    fingerprint-enabled: true          # 机器指纹绑定校验（签发时绑定了指纹才生效）
    allow-startup-without-license: false  # true = 无授权也能启动，但受保护能力被拦截
```

把签发端交付的授权串完整写入 `license.dat`（内容就是授权串本身，不要多空格/换行）。

> **启动策略**：默认缺授权直接启动失败，暴露原因避免「以为受保护实则裸奔」；`allow-startup-without-license: true` 时以「非法不可用」状态启动，受保护接口拦截、其余照常，适配先启动后补授权的交付流程。

### 5.3 保护接口：@LicenseCheck

在需要保护的接口方法（或整个 Controller/Service 类）上标注即可，进入前自动校验：

```java
@LicenseCheck                                // 基础可用性：合法或宽限期内才放行
@GetMapping("/protected")
public R<String> protectedApi() { ... }

@LicenseCheck(module = "report")             // 额外要求授权含 report 模块
@GetMapping("/report")
public R<String> report() { ... }

@LicenseCheck(online = true)                 // 每次调用触发一次联机回验（感知吊销）
@GetMapping("/online")
public R<String> online() { ... }
```

类上标注对其下所有方法生效，方法上可覆盖。校验不通过抛授权异常，由全局异常处理器转统一响应体。

### 5.4 查询授权状态

注入 `LicenseManager` 读取当前状态：

```java
LicenseStatus status = licenseManager.getStatus();   // LEGAL / GRACE / ILLEGAL
boolean usable = status.isUsable();
LicenseContent content = licenseManager.getContent(); // subject/licenseId/expireAt/modules/quotas...
```

### 5.5 读取额度与扩展参数

消费端按业务需要读取授权里的额度和扩展参数（无授权时 `getContent()` 为 `null`，先判空再读）：

```java
LicenseContent content = licenseManager.getContent();
if (content == null) {
    // 无授权：业务自己决定是否拦截
}

// 额度：返回该额度上限（Long），未配置返回 null（视为不限）
Long deviceLimit = content.quota("device");
if (deviceLimit != null && currentDeviceCount > deviceLimit) {
    throw new LicenseException(LicenseErrorCode.LICENSE_QUOTA_EXCEEDED,
        "设备数超出授权额度 " + deviceLimit + "，请联系供应方扩容");
}

// 扩展参数：返回参数值（String），未配置返回 null
String edition = content.attribute("edition");
boolean enterprise = "enterprise".equals(edition);   // 按值分支开放高级能力
```

> 额度只是「上限」的契约：`currentDeviceCount` 这类实际使用计数须由业务自己维护（入库/缓存自增），授权只负责告诉你上限，不负责计数。

### 5.6 机器指纹

```java
String fp = MachineFingerprint.current();   // 采集主机特征生成 SM3 摘要
```

在签发时把该值填入授权「机器指纹」即完成绑定；换机器后授权失效（可在签发端重新签发绑定新机器）。

---

## 6. 联机校验：吊销实时闭环

> 离线模式**感知不到吊销**（授权文件没删，验签仍通过）。需要「吊销立即生效」时接入本环节。
> 联机失败默认**放行+告警**，不会因签发端故障拖垮业务；只有签发端**明确判定无效**才拦截。

### 6.1 消费端配置

```yaml
ypbin:
  license:
    online:
      base-url: ${LICENSE_ONLINE_BASE_URL:http://localhost:8080}   # 签发端地址
      access-key: ${LICENSE_ONLINE_ACCESS_KEY:}                    # 开放应用 Access Key（签发端注册）
      secret-key: ${LICENSE_ONLINE_SECRET_KEY:}                    # 开放应用 Secret Key（参与签名）
      timeout: 3s                                                  # 可选，默认 3s
```

配了 `base-url` 即自动装配 HTTP 联机校验（`HttpRemoteVerifyProvider`），且要求 `access-key / secret-key` 齐备（缺失启动即报错暴露配置问题）；未配 `base-url` 则纯离线。

### 6.2 三种触发方式（可叠加）

1. **接口调用时**：`@LicenseCheck(online = true)`，每次调用实时回验；
2. **登录时**：引入 `ypbin-starter-security`，`StpUtil.login` 触发登录回验（吊销后登录被拦）；
3. **定期任务**：引入 `ypbin-starter-job`，注册执行器 `licenseOnlineVerify` 的 30s 定时任务，吊销后持续告警。

### 6.3 行为策略

| 场景 | 消费端行为 |
|---|---|
| 签发端明确返回 `valid=false`（已吊销/授权不存在/签名无效/指纹不符） | **拦截**（错误码 7008） |
| 网络不可达 / 超时 / 非 200 / 响应异常 | **放行** + 日志 warn 告警 |
| 签名/应用密钥未配置或错误 | 签发端签名校验不过 → 返回 `valid=false`（不静默放行） |

> 消费端联机接口路径：`GET {签发端}/open/license/verify?licenseId=..&fingerprint=..&accessKey=..&timestamp=..&nonce=..&sign=..`。鉴权为开放应用 AK/SK 接口签名（四件套 `accessKey/timestamp/nonce/sign`，含时间窗与 nonce 防重放），由 `ypbin-starter-sign` 的 `SignClient` 自动生成，业务侧无需手工拼。该接口已在签发端安全放行清单中，无需登录。

---

## 7. 错误码对照

消费端校验失败统一 HTTP 200 + 业务码（`code` 字段），7xxx 为授权错误区段：

| code | 含义 | 触发场景 |
|---|---|---|
| 7001 | 未检测到有效授权 | 无授权文件 |
| 7002 | 授权文件已损坏或无法解析 | 授权串乱写 / 格式错误 |
| 7003 | 授权签名验证失败 | 公钥配错 / 授权串被篡改 |
| 7004 | 授权与运行环境不匹配 | 机器指纹不符，禁止跨机器使用 |
| 7005 | 授权尚未到生效时间 | `effectiveAt` 未到 |
| 7006 | 授权已过期 | 过期且宽限期耗尽 |
| 7007 | 检测到系统时间异常 | 系统时间被回拨篡改 |
| 7008 | 联机授权校验未通过 | 已吊销 / 授权不存在 / 签名无效 / 指纹不符 |
| 7009 | 功能模块未授权 | 访问了授权范围外的模块 |
| 7010 | 已达到授权额度上限 | 设备数/用户数等超出 `quotas` |

---

## 8. 安全须知

1. **私钥不落库、不下发**：签发端 SM2 私钥只存在于部署环境变量；消费端只需公钥 + SM4 密钥。私钥外泄 = 任何人都能签发授权。
2. **密钥一次性展示**：「生成密钥」弹窗仅返回一次，务必离线保管；生产环境每套授权用独立密钥对。
3. **应用密钥保密**：`access-key / secret-key` 用环境变量注入，不要提交到代码库。Secret Key 泄露后他人可伪造该应用的联机请求，须在签发端「开放应用管理」重置密钥；每应用独立密钥、可随时禁用，泄露只影响单一应用。**生产勿用内置演示应用（V2 种子）的密钥**。签发时按被授权方自动创建的联机应用，其 Secret Key 在授权交付信息中可见（需 `system:license:list` 权限）；该应用与手工建的应用一样，可在开放应用管理随时重置密钥/禁用。
4. **时钟回拨保护**：消费端检测到系统时间回拨超过阈值即判非法（7007），防止通过改时间绕过过期。
5. **授权串防篡改**：SM2 签名保证授权串任何字节被改都会验签失败（7003）。
6. **变更授权需重新签发**：任何字段修改都走「编辑 → 重新提交 → 重新审批」，签发后授权串变化、旧串失效。

---

## 9. 常见问题

**Q：消费端换了机器，授权还能用吗？**
看签发时是否绑定了指纹。绑定了则换机即失效，需在签发端重新签发绑定新指纹的授权；未绑定则任意机器可用。

**Q：吊销后消费端还放行？**
离线模式感知不到吊销。接入联机校验（第 6 章）后，吊销通过接口调用/登录/定期任务实时生效。

**Q：内容复杂时授权码超长 / 报「授权信息过多」？**
授权码超 768 字符上限，改用授权文件 `FILE` 交付即可。

**Q：想先让客户跑起来再补授权？**
消费端配 `allow-startup-without-license: true`，无授权也能启动，受保护能力被拦截，用 `/fingerprint` 取指纹去签发后重启即可。

**Q：签发端不可达时消费端会崩吗？**
不会。联机失败按「放行+告警」处理，只有签发端明确判无效才拦截。

**Q：联机校验的 AK/SK 从哪来？泄露了怎么办？**
审批通过签发授权时，系统按被授权方 `subject` 自动创建/复用开放应用，AK/SK 在交付信息中展示（同主体续期复用、保持稳定）；也可在「授权管理 → 开放应用」手工建独立应用。Secret Key 泄露可到应用管理**重置密钥**或**禁用**该应用，只影响该应用、不影响其他消费端；生产环境不要用内置演示应用（V2 种子）的密钥。

**Q：admin 自己审批为什么报错？**
双人复核设计：审批人必须 ≠ 创建人，须用 `approver` 登录审批。

---

## 10. 术语表

| 术语 | 含义 |
|---|---|
| 签发端 / 供应方 | 用 admin 管理台签发授权的角色 |
| 消费端 / 被授权方 | 引入 `ypbin-starter-license` 运行授权校验的软件 |
| 授权串 / authCode | 签发产物，Base64 编码的加密授权数据（授权码或 .lic 内容） |
| SM2 / SM4 / SM3 | 国密算法：签名验签 / 对称加密 / 摘要 |
| 机器指纹 | 主机特征生成的 SM3 摘要，用于绑定机器 |
| 宽限期 | 过期后仍可运行的缓冲天数（期间状态为「非法可用」） |
| 联机校验 | 消费端向签发端实时回验授权状态，可感知吊销 |
| 放行+告警 | 联机失败时的降级策略：不阻断业务，仅日志告警 |
